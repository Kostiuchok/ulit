# Роадмап тестування — Knyha Platform

> Від поточного стану (типчек + білд-гейт в CI, тонкий шар unit/e2e) до повного покриття критичних флоу перед і після Альфи.
> Формат чекліста — став `[x]` і дописуй коментар прямо під пунктом (напр. `> нотатка`).
> Дата створення: 2026-09-15. Оновлювати при кожній суттєвій зміні тестового покриття.

---

## 📊 Поточний стан

- [x] Типи (`tsc --noEmit`, `pnpm typecheck`) — весь монорепо, блокує CI
- [x] Продакшн-білд (`pnpm --filter web build`) — реальний `next build`, той самий що на VPS, блокує CI (додано 2026-09-15)
- [x] Unit (API, vitest) — `auth.integration`, `isbn.service`, `schemas`, `shared-schemas` (79 тестів разом), блокує CI
- [ ] Лінт (`next lint`) — є, але `continue-on-error: true`, не блокує
- [ ] Unit (worker) — **немає жодного тесту**
- [ ] E2E (Playwright, проти живого прода) — реєстрація/логін/каталог/кошик-редірект/`/orders/:id`/кілька author-флоу за фікстурою — є, але `continue-on-error: true`, запускається лише на push у `main`
- [ ] Адмін-панель — **немає жодного тесту** (лише непряма перевірка "не-адмін не пройде" з боку автора)
- [ ] Друк-пайплайн (WeasyPrint/PDF) — **немає жодного тесту**, весь журнал прод-багів (CLAUDE.md #19-27) знайдено вручну
- [ ] Візуальна регресія — немає
- [ ] Навантаження — немає

> Головний висновок: усе, що є, — тонкий каркас навколо auth + магазину. Дві найбільші поверхні продукту — **адмінка** і **друк-пайплайн** (найбільше джерело задокументованих прод-інцидентів) — без жодного автотесту.

---

## 🗺️ Мапа користувача (автор + покупець)

- [x] Реєстрація / вхід — `/register`, `/register/check-email`, `/verify-email`, `/login`
  - e2e є
- [x] Кабінет автора — список книг — `/dashboard/books`
  - e2e є (за фікстурою)
- [ ] Wizard публікації (5 кроків) — `/dashboard/books/new`
  - Покрито лише крок 1
  - Бракує: E2E повний прохід до публікації
  - 🔴 високий пріоритет
- [ ] Завантаження рукопису (docx → PDF/EPUB/FB2/MOBI) — upload у wizard
  - Бракує: E2E з реальним docx-файлом + перевірка статусу конвертації
  - 🔴 високий пріоритет
- [ ] Онлайн-редактор рукопису (TipTap) — `/dashboard/books/[id]/manuscript`
  - Бракує: E2E — набір тексту, вставка зображення, autosave, live page-break маркер
  - 🟡 середній пріоритет
- [ ] Передперегляд рукопису (флипбук, react-pdf) — `/dashboard/books/[id]/manuscript/preview`
  - Покрито непрямо (сьогоднішній баг з `pdf.worker` знайдено вручну, не тестом)
  - Бракує: E2E — PDF реально рендериться, немає console errors
  - 🔴 високий пріоритет
- [ ] Редактор обкладинки (Fabric.js) — `/dashboard/books/[id]/cover`
  - Бракує: E2E — завантаження/переміщення/кадрування елементів
  - 🟡 середній пріоритет
- [ ] ISBN/УДК форма — Вихідні дані книги
  - `isbn.service` вже покритий unit-тестами
  - Бракує: E2E самої форми
  - 🟢 частково є
- [ ] Стратегія дистрибуції (KDP Select vs Wide) — крок 4 wizard
  - Бракує: unit на `kdpSelectExpiry` gate-логіку
  - 🟡 середній пріоритет
- [ ] Публікація книги — кнопка "Опублікувати"
  - Бракує: E2E — чернетка → PUBLISHED → видно в магазині
  - 🔴 високий пріоритет
- [ ] Договір автора / реквізити — `/dashboard/settings/contract`
  - Бракує: E2E — обов'язкові поля, підписання
  - 🟡 середній пріоритет
- [ ] Налаштування профілю — `/dashboard/settings`
  - e2e є (за фікстурою, лише рендер)
  - Бракує: форма збереження ПІБ/slug
  - 🟢 низький пріоритет
- [x] Магазин — каталог/пошук/фільтри — `/books`
  - e2e є
- [ ] Сторінка книги / автора — `/books/[slug]`, `/authors/[slug]`
  - e2e є частково
  - Бракує: формати/ціни/уривок-рідер
  - 🟡 середній пріоритет
- [ ] Кошик → checkout → LiqPay — `/cart`, `/checkout/[orderId]`
  - Навмисно поза e2e (реальні платежі)
  - Бракує: LiqPay sandbox-тест або мок callback
  - 🔴 високий пріоритет
- [ ] Статус замовлення — `/orders/[id]`
  - e2e є (сьогодні знайдено й виправлено реальний баг — стуковане "loading" для розлогіненого користувача)
  - Бракує: успішний PAID-флоу (потребує sandbox)
  - 🟡 середній пріоритет
- [ ] Роялті/виплати автора — `/dashboard/settings/royalties`, `/dashboard/settings/bonuses`
  - Бракує: E2E рендеру + unit розрахунку
  - 🟢 низький пріоритет (Фаза 2 продукту)
- [x] Юридичні сторінки — `/terms`, `/privacy`, `/author-agreement`
  - e2e є

---

## 🛡️ Мапа адміна

- [ ] Вхід адміна / гард ролі — `admin/layout.tsx`
  - Покрито непрямо (тест "не-адмін не пройде" з боку автора)
  - Бракує: E2E — реальний адмін логіниться й бачить дашборд
  - 🔴 високий пріоритет
- [ ] Dashboard (KPI, черга, події) — `/admin/dashboard`
  - Бракує: E2E рендеру + бейджі черги
  - 🟡 середній пріоритет
- [ ] Модерація книг — `/admin/books`, схвалити/відхилити
  - Бракує: E2E — PENDING → APPROVED/REJECTED, книга з'являється/зникає в магазині
  - 🔴 високий пріоритет
- [ ] Відправка книги на сервіси — `/admin/books/[id]/distribute`
  - Бракує: E2E форми + unit `distribution.service` (ZIP/метадані)
  - 🔴 високий пріоритет
- [ ] Черга дистрибуції / масова відправка — `/admin/distribution/queue`, `/bulk`
  - Бракує: E2E рендеру черги
  - 🟡 середній пріоритет
- [ ] Заявки на API-доступ — `/admin/applications/*`
  - Бракує: E2E CRUD статусів заявки
  - 🟢 низький пріоритет
- [ ] Автори (адмін-перегляд) — `/admin/authors`, `/admin/authors/[id]`
  - Бракує: E2E рендеру
  - 🟢 низький пріоритет
- [ ] Виплати роялті — `/admin/royalties`
  - Бракує: unit розрахунку суми/статусу + E2E "позначити оплаченим"
  - 🟡 середній пріоритет
- [ ] Черга виплат (withdrawal) — `/admin/withdrawal-queue`
  - Бракує: E2E рендеру + unit статус-машини
  - 🟡 середній пріоритет
- [ ] УДК-черга — `/admin/udk-queue`
  - Бракує: E2E — книга з'являється/зникає за станом `udcCode`/ISBN (тут уже був живий баг, задокументований у журналі)
  - 🔴 високий пріоритет
- [ ] Друк-замовлення — `/admin/print-orders`
  - Бракує: E2E рендеру + unit `print-cost.ts`
  - 🟡 середній пріоритет
- [ ] Налаштування вартості друку — `/admin/settings/print-cost`
  - Бракує: unit формули розрахунку ціни
  - 🟡 середній пріоритет
- [ ] Сервіси (toggle on/off) — `/admin/services`
  - Бракує: E2E toggle
  - 🟢 низький пріоритет
- [ ] Документи видавця — `book-chamber.ts`, `publisher-documents.ts`
  - Бракує: unit генерації файлів для Книжкової палати
  - 🟡 середній пріоритет

---

## 🚦 Фази роадмапу (порядок виконання)

### Фаза 1 — Розблокувати вже написане (найдешевша, зроблено на 90%)
- [x] CI реально запускає `next build`, не лише typecheck (2026-09-15)
- [x] `auth-publish.spec.ts` / `store-purchase.spec.ts` полагоджено, усі активні тести зелені (2026-09-15)
- [ ] **За тобою**: створити 1 реальний підтверджений акаунт-автора → секрети `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD` у GitHub — розблоковує 5 вже написаних, але заскіпаних тестів

### Фаза 2 — Закрити 🔴 високий пріоритет з мапи користувача
- [ ] E2E: повний wizard до публікації (усі 5 кроків, реальний тестовий docx)
- [ ] E2E: передперегляд рукопису рендериться без console errors
- [ ] E2E: чернетка → публікація → книга видима в каталозі
- [ ] Sandbox/мок для LiqPay callback (`POST /api/payments/liqpay/callback`) — щонайменше unit на підпис/суму, в ідеалі integration з LiqPay sandbox-ключами

### Фаза 3 — Адмінка з нуля
- [ ] Другий фікстур-акаунт: `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD` (роль ADMIN)
- [ ] E2E: модерація (approve/reject), видимість у черзі УДК
- [ ] E2E: `/admin/books/[id]/distribute` форма
- [ ] Unit: `distribution.service` (ZIP-генерація, метадані пакета)

### Фаза 4 — Unit-тести для мовчазних відмов у бекенді
> Сервіси/джоби, де баг не кидає помилку, а тихо ламає результат (клас уже описаний у журналі CLAUDE.md #9, #10, #12)
- [ ] `liqpay.service.ts` — перевірка підпису, розрахунок суми
- [ ] `print-cost.ts` — формула ціни друку
- [ ] BullMQ job-dedup (журнал #12: повторний `add()` з тим самим `jobId` мовчки не запускає нову джобу) — регресійний unit саме на цю поведінку
- [ ] `storage.service.ts` `getSignedUrl()` — регресія на журнал #9 (Mixed Content)

### Фаза 5 — Друк-пайплайн: golden-file регресія
> Найбільше джерело задокументованих прод-багів (WeasyPrint pagination, title page, float-зображення — журнал #19-27), і досі жодного автотесту
- [ ] Зафіксувати 2-3 "еталонні" рукописи (короткий/довгий заголовок, зображення align=left/right, багатосторінковий) → рендерити `generate-pdf-print.ts` у CI-подібному контейнері → порівнювати кількість сторінок / наявність відомих поламаних патернів (подвійна порожня сторінка, обрізане зображення) як regression-guard
- [ ] Мінімум: тест "рендер не падає й дає N±1 сторінок" — дешево, ловить `weasyprint`-креші при апгрейді пакета (журнал #27 показав, що апстрім-апдейти сюди прилітають мовчки)

### Фаза 6 — Зробити гейти обов'язковими
- [ ] Коли Фаза 1-3 стабільні (кілька тижнів зелених ранів) — прибрати `continue-on-error: true` з e2e job у `ci.yml`
- [ ] Розглянути, чи лінт теж пора зробити блокуючим

### Фаза 7 — Візуальна регресія (пізніше, не для Альфи)
- [ ] Редактор обкладинки / манускрипт-редактор — скріншот-diff (Playwright `toHaveScreenshot`)

### Фаза 8 — Навантаження (перед масштабуванням поза Альфу)
- [ ] Базовий load-тест на конвертацію docx (черга BullMQ, concurrency: 2 — де стеля?)
- [ ] Load-тест на `/api/store/books` (публічний каталог, найбільший трафік)

---

## 🧭 Правила

- [ ] Перед push, якщо змінювався `apps/web` — `pnpm --filter web build` локально, коли є вільна пам'ять (журнал #13/#15). CI тепер теж це ловить, але локальна перевірка швидша при ітерації
- [ ] Новий job з детермінованим `jobId` — обов'язково unit на dedup-поведінку (журнал #12), інакше баг повториться в наступному job-типі
- [ ] Новий друк-специфічний CSS/розмітка — рендерити реальним файлом з майже повною сторінкою (журнал #19/#20), не покладатись на "має підтримуватись" для WeasyPrint-специфічних фіч (flexbox, `counter-reset`, floats — усі мали задокументовані сюрпризи)
- [ ] Кожен новий бекенд-модуль з префіксом — перевірити, що доданий у `next.config.mjs` rewrites (журнал #4/#17), і мати хоча б один E2E-запит, що реально проходить через браузер, не лише `curl` напряму до контейнера (журнал #17/#18)
