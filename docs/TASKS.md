# TASKS.md — Knyha Platform

> Статуси: `[ ]` не розпочато · `[~]` в процесі · `[x]` готово
> Гілки: `feat/<task-id>-short-name`

---

## 🚀 ФАЗА 0 — Ініціалізація ✅

- [x] **T-001** Turborepo monorepo (pnpm workspaces + turbo.json)
- [x] **T-002** `apps/web` — Next.js 14 + Tailwind CSS
- [x] **T-003** `apps/api` — Fastify + TypeScript + AppError
- [x] **T-004** `packages/shared-types` — спільні TypeScript інтерфейси
- [x] **T-005** ESLint + Prettier для всього monorepo
- [x] **T-006** `infra/docker-compose.yml` для dev (postgres, redis, minio)
- [x] **T-007** `.env.example` для `web` та `api`
- [x] **T-008** GitHub Actions CI (lint + typecheck на кожен PR)

## 🗄️ ФАЗА 1 — Поточна фаза

**Поточний статус:** Фаза 1 завершена ✅ → Наступний крок: Фаза 2 (T-201 профіль автора)

---

## 🗄️ ФАЗА 1 — База даних та авторизація

- [x] **T-101** Prisma schema (User, Book, Order, OrderItem, Royalty + всі enum)
- [x] **T-102** Prisma міграції
- [x] **T-103** Seed скрипт (тестовий автор + 2 книги)
- [x] **T-104** NextAuth.js v5 — Email/Password стратегія
- [x] **T-105** Google OAuth провайдер
- [x] **T-106** `POST /api/auth/register`
- [x] **T-107** `POST /api/auth/login` — JWT
- [x] **T-108** JWT middleware для захищених routes
- [x] **T-109** Admin middleware — guard для `/admin/*` routes (role: ADMIN)
- [x] **T-110** Сторінки `/login` та `/register` (RHF + Zod)
- [x] **T-111** Зберігати `contractAcceptedAt` при першій публікації (при T-703)
- [x] **T-112** Google OAuth в production — додати `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` до `.env.production` на VPS; перебудувати `knyha-web`; перевірити `AUTH_URL` callback у Google Console
- [ ] **T-113** Sign in with Apple — додати `next-auth/providers/apple` до `auth.ts` за тим самим умовним патерном, що й Google (провайдер вмикається лише якщо є credentials); потрібні від автора: Apple Developer Program акаунт, Services ID (redirect `https://ulit.render.ua/api/auth/callback/apple`), Team ID, Key ID, `.p8` приватний ключ; env: `APPLE_CLIENT_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY`; кнопка "Увійти через Apple" на `/login` поруч з Google

---

## 👤 ФАЗА 2 — Профіль автора

- [x] **T-201** `GET/PATCH /api/users/me`
- [x] **T-202** `POST /api/users/me/avatar` (MinIO)
- [x] **T-203** Сторінка `/dashboard/settings`
- [x] **T-204** Компонент завантаження аватара (react-image-crop)
- [x] **T-205** Унікальний slug для автора

---

## 📚 ФАЗА 3 — Управління книгами (Dashboard)

- [x] **T-301** `GET /api/books` — книги автора
- [x] **T-302** `POST /api/books` — створити чернетку
- [x] **T-303** `GET /api/books/:id`
- [x] **T-304** `PATCH /api/books/:id` — метадані
- [x] **T-305** `DELETE /api/books/:id`
- [x] **T-306** Сторінка `/dashboard/books` — картки книг
- [x] **T-307** Сторінка `/dashboard/books/new` — wizard 5 кроків
- [x] **T-308** Компонент `BookWizard` — прогрес-бар + кроки

---

## 📄 ФАЗА 4 — Завантаження та конвертація

- [x] **T-401** MinIO клієнт (`storage.service.ts`)
- [x] **T-402** `POST /api/books/:id/upload-docx`
- [x] **T-403** Компонент `DocxUploader` (drag & drop + прогрес)
- [x] **T-404** BullMQ + Redis налаштування
- [x] **T-405** `apps/worker/Dockerfile` — Ubuntu 22.04 + Node 20 + LibreOffice + Pandoc + Ghostscript + Calibre
- [x] **T-406** Job `convert-docx-to-pdf` — LibreOffice → PDF (стиснений)
- [x] **T-407** Job `generate-pdf-print` — Ghostscript → PDF/X-3 (CMYK, 300 DPI, bleed 3мм)
- [x] **T-408** Job `generate-epub` — Pandoc → EPUB 3
- [x] **T-409** Job `generate-fb2` — Calibre → FB2
- [x] **T-410** Job `generate-mobi` — Calibre → MOBI/AZW3
- [x] **T-411** `publishing.service.ts` — orchestrator jobs
- [x] **T-412** `POST /api/books/:id/job-status` — оновлення статусу
- [x] **T-413** Polling на фронті — прогрес кожного формату окремо
- [x] **T-414** Валідація print PDF (> 10MB — без стиснення)

### Фаза 4b — UX прогресу завантаження

- [x] **T-415** Прогрес-бар завантаження DOCX: використати `XMLHttpRequest` з `upload.onprogress` замість `fetch` у `DocxUploader`, відображати відсоток (0–100%) під час передачі файлу на сервер; після завантаження показати "Обробляється…" і перейти до існуючого polling статусу конвертації

---

## 🎨 ФАЗА 5 — Редактор обкладинки

- [x] **T-501** Fabric.js canvas — компонент `CoverDesigner`
- [x] **T-502** Завантаження власного зображення
- [x] **T-503** Інструменти: текст, колір, шрифт, позиція
- [x] **T-504** Unsplash API — безкоштовні фонові зображення
- [x] **T-505** Готові шаблони (мінімум 5)
- [x] **T-506** Експорт PNG (300 DPI)
- [x] **T-507** `POST /api/books/:id/upload-cover` → MinIO

### Фаза 5b — Виправлення та покращення редактора

- [x] **T-508** Власне зображення як фон не працює: кнопка "Завантажити фото" у вкладці "Фон" натискається, але файл не завантажується — виправити `handleFileUpload` у `CoverDesignerCanvas.tsx`
- [x] **T-509** Undo/Redo (Ctrl+Z / Ctrl+Y) — при змінах тексту, позиції, розтягуванні об'єктів; реалізувати через стек станів canvas JSON (Fabric.js `toJSON` / `loadFromJSON`)
- [x] **T-510** Snap до кутів при повороті — крок 10°, спрацьовує якщо об'єкт повертається в межах ±3° від кратного 10°; реалізувати через `object:rotating` event у Fabric.js
- [x] **T-511** Додавання та редагування прямокутника: кнопка "Додати прямокутник" → `fabric.Rect` на canvas; панель властивостей активного прямокутника: колір заливки, колір обводки, товщина обводки, прозорість (opacity 0–100%); видалення через Delete/кнопку

---

## 📡 ФАЗА 6 — Стратегія дистрибуції (KU vs Широке)

- [x] **T-601** UI вибору стратегії на кроці 4 wizard (WIDE | KDP_SELECT)
- [x] **T-602** Зберігати `distributionStrategy` + `kdpSelectExpiry` в БД
- [x] **T-603** Логіка: якщо KDP_SELECT — при публікації знімати з D2D та сайту
- [x] **T-604** Нотифікація автору за 7 днів до закінчення KDP Select (email job)
- [x] **T-605** UI у кабінеті автора — статус ексклюзивності + дата закінчення
- [x] **T-606** Вибір платформ розповсюдження автором — замість бінарного "Широке / KDP Select" дати автору чекбокси конкретних платформ: **Draft2Digital**, **Amazon KDP**, **Google Play Books**, **Магазин Ulit** (завжди увімкнений); зберігати вибір у `distributionChannels String[]` у моделі `Book` (міграція!); логіка публікації: якщо обрано KDP без D2D/Google — автоматично виставляти `KDP_SELECT`, інакше `WIDE`; UI — на кроці 4 wizard замінити radio-кнопки на картки-чекбокси з назвою платформи, роялті % та коротким описом умов; у кабінеті автора показувати тільки обрані платформи в блоці розповсюдження. _Повна версія з матрицею роялті та цінами: T-1924._

---

## 🔖 ФАЗА 7 — ISBN та публікація

- [x] **T-701** `isbn.service.ts` — mock для MVP, реальна Книжкова палата — Фаза 2
- [x] **T-702** Валідація перед публікацією (обкладинка + файл + метадані + ціна)
- [x] **T-703** `POST /api/books/:id/publish`
- [x] **T-704** Статуси: DRAFT → PROCESSING → REVIEW → PUBLISHED
- [x] **T-705** Email автору після публікації
- [x] **T-706** Сторінка підтвердження з ISBN і посиланням

---

## 🏪 ФАЗА 8 — Публічний магазин

- [x] **T-801** `GET /api/store/books` — каталог (фільтри: жанр, мова, формат)
- [x] **T-802** `GET /api/store/books/:slug`
- [x] **T-803** `GET /api/store/authors/:slug`
- [x] **T-804** Головна сторінка — банер + нові + популярні
- [x] **T-805** Сторінка книги — обкладинка, опис, ISBN, формати, ціна
- [x] **T-806** Сторінка автора — фото, біо, книги
- [x] **T-807** `BookCard` компонент
- [x] **T-808** Пошук (PostgreSQL `tsvector`)
- [x] **T-809** Пагінація (cursor-based)
- [x] **T-810** SEO (Open Graph, JSON-LD)

---

## 💳 ФАЗА 9 — Замовлення та платежі

- [x] **T-901** `POST /api/orders`
- [x] **T-902** `GET /api/orders/:id`
- [x] **T-903** LiqPay інтеграція — форма оплати
- [x] **T-904** `POST /api/payments/liqpay/callback`
- [ ] **T-905** Stripe Payment Intent (Фаза 2)
- [ ] **T-906** `POST /api/payments/stripe/webhook`
- [ ] **T-909** WayForPay інтеграція — форма оплати (Фаза 2)
- [ ] **T-910** `POST /api/payments/wayforpay/callback`
- [x] **T-907** Email з signed URLs після оплати (48 год)
- [x] **T-908** Сторінка `/orders/:id` — статус для покупця

---

## 📖 ФАЗА 10a — Повний перегляд книги (Book Viewer)

> Після конвертації DOCX автор бачить як виглядатиме книга в фінальному вигляді:
> обкладинка → сторінки → задня обкладинка з фото і біо автора.

> **Статус: реалізовано** (backend + frontend). Дизайн-рев'ю Figma (`nOXLPYS4l4mxOkCINYdQdT`, сторінка "Book Viewer") показало, що наступна ітерація вигляду вже описана в **T-1929** (розворот вбудований у дашборд, повзунок-навігатор, перемикач 3D/PDF) — поточний повноекранний модал зі стрічкою мініатюр закриває оригінальні критерії T-1013, але не збігається з новим вайрфреймом.

### 10a-i — Backend

- [x] **T-1010** `GET /api/books/:id/pages` — список URL сторінок: повертає масив `{ page: number, url: string }` де URL — це signed/public MinIO URL PNG-рендерів; якщо PNG ще не згенеровані — повертає `{ status: "PROCESSING" }` і запускає job
- [x] **T-1011** Job `generate-page-thumbnails` у worker: Ghostscript рендерить кожну сторінку PDF у PNG (`gs -dNOPAUSE -dBATCH -sDEVICE=png16m -r96 -sOutputFile=page-%03d.png input.pdf`); завантажити в MinIO `public/pages/BOOKID/page-001.png` тощо. _Примітка: реалізовано з `-r96` (роздільність з розміру PDF-сторінки), а не фіксованим 800×1200px — переглянути, якщо якість недостатня._
- [x] **T-1012** `GET /api/books/:id/back-cover-data` — повертає `{ authorName, avatarUrl, bio }` для генерації задньої обкладинки на фронті

### 10a-ii — Frontend

- [x] **T-1013** Сторінка `/dashboard/books/:id/preview` — Book Viewer (повноекранний режим): `BookViewer.tsx` — обкладинка → сторінки → задня обкладинка, стрілки ←→, keyboard nav, стрічка мініатюр знизу, кнопка "Закрити". _Наступна ітерація вигляду (вбудований у дашборд розворот + повзунок + 3D/PDF) — див. T-1929._
- [x] **T-1014** Skeleton / прогрес завантаження сторінок — `preview/page.tsx` полить `GET /api/books/:id/pages` кожні 3с поки `status !== "DONE"`
- [x] **T-1015** Кнопка "Переглянути книгу" на сторінці `/dashboard/books/:id` — активна тільки після завершення конвертації (`hasConversion = !!book?.pdfUrl`)
- [x] **T-1016** Задня обкладинка — компонент `BackCoverPage`: аватар автора, ім'я, біо, логотип платформи

### 10a-iii — Schema

- [x] **T-1017** Prisma: `pageCount Int?` + `pagesGeneratedAt DateTime?` на `Book` — API перевіряє це поле щоб не запускати job повторно

---

## 👁 ФАЗА 10 — Preview уривку

- [x] **T-1001** Автор виділяє уривок (мишею або діапазон сторінок)
- [x] **T-1002** Зберігати `previewStart` / `previewEnd` в БД
- [x] **T-1003** EPUB reader для покупця (`epubjs`) з темами + paywall

---

## 🛡 ФАЗА 11 — Адмін-панель

- [x] **T-1101** Layout `/admin` — sidebar + topbar (окремий від auth guard)
- [x] **T-1102** `/admin/dashboard` — KPI картки + останні події + черга
- [x] **T-1103** `/admin/books` — таблиця + фільтри (статус, сервіс, жанр)
- [x] **T-1104** Схвалення книги: `PATCH /api/admin/books/:id/approve`
- [x] **T-1105** Відхилення книги з причиною: `PATCH /api/admin/books/:id/reject`
- [x] **T-1106** `/admin/books/[id]/distribute` — форми D2D + KDP + Google
- [x] **T-1107** Чеклист готовності книги (EPUB ✅ | обкладинка ✅ | ISBN ✅)
- [x] **T-1108** `POST /api/admin/books/:id/export-package` — ZIP (EPUB + cover + metadata.csv)
- [x] **T-1109** Статуси відправки: `ExternalStatus` (NOT_SENT | SENT | PUBLISHED | ERROR)
- [x] **T-1110** `/admin/distribution/queue` — черга книг готових до відправки
- [x] **T-1111** `/admin/distribution/bulk` — масова відправка (ZIP з кількох книг)
- [x] **T-1112** `/admin/applications/kdp-api` — форма заявки KDP Selling Partner API
- [x] **T-1113** `/admin/applications/google-books` — форма заявки Google Books Partner API
- [x] **T-1114** `/admin/applications/d2d-partner` — лист до D2D Business Development
- [x] **T-1115** `/admin/royalties` — список виплат + поріг 500 грн + кнопка Виплатити
- [x] **T-1116** `POST /api/admin/royalties/:id/pay` — відмітити як виплачено
- [x] **T-1117** `/admin/royalties/export` — CSV для бухгалтерії
- [x] **T-1118** `/admin/services` — toggle on/off кожного сервісу дистрибуції
- [x] **T-1119** `/admin/authors` — список авторів + статус договору
- [x] **T-1120** `/admin/authors` — розширена таблиця: email, дата реєстрації, кількість книг, статус договору, остання активність; пошук + фільтр по статусу
- [x] **T-1121** `DELETE /api/admin/users/:id` — видалення акаунту автора (каскадно: книги, замовлення, роялті); підтверджуючий модал на фронті
- [x] **T-1122** `/admin/authors/:id` — сторінка деталей автора: профіль + повний список книг зі статусами конвертації та дистрибуції
- [x] **T-1123** Завантаження файлів книги з панелі автора: кнопки Download для кожного формату (EPUB, FB2, MOBI, PDF, Print PDF) → signed MinIO URL (48 год)
- [ ] **T-1124** Чекліст вимог платформ для адміна — на сторінці `/admin/books/:id/distribute` додати панель автоматичної перевірки відповідності книги вимогам кожної платформи; **автоматичні перевірки**: наявність ISBN ✓, обкладинка (мін. 1600px по довшій стороні) ✓, EPUB файл ✓, опис (≥ 150 символів) ✓, жанр вказано ✓, мова вказана ✓, ціна встановлена ✓, pageCount > 0 ✓; **платформо-специфічні вимоги**: Amazon KDP — ISBN, EPUB або MOBI, опис ≥ 250 символів, обкладинка ≥ 1600px; Draft2Digital — EPUB, ISBN необов'язковий, опис ≥ 50 символів; Google Play Books — EPUB, ISBN, обкладинка ≥ 1400px; **UI**: три секції-акордеони (по платформі), кожна вимога — рядок з іконкою ✓/✕/⚠ та поясненням що виправити; підсумок "Готово до відправки" або "N вимог не виконано"; кнопка "Відхилити" автоматично підставляє у поле причини перелік незадоволених вимог. _Споріднено: T-1907 (прогрес-індикатор на стороні автора)._

---

## 🔐 ФАЗА 16 — Сесії та UX авторизації

- [x] **T-1601** Persistent session — користувач не повинен логінитись після кожного переходу між сторінками; перевірити cookie `maxAge`, `AUTH_SECRET` на VPS та `trustHost` за Caddy reverse proxy
- [x] **T-1602** Підтвердження email при реєстрації — після `POST /api/auth/register` НЕ створювати активний акаунт одразу; зберігати `emailVerified Boolean @default(false)` + `emailVerificationToken String?` у моделі `User`; надсилати лист із посиланням `/verify-email?token=<uuid>`; `GET /api/auth/verify-email?token=` — ставить `emailVerified: true`, інвалідує токен; до підтвердження — `POST /api/auth/login` повертає 403 `EMAIL_NOT_VERIFIED`; на фронті — після реєстрації показувати екран "Перевірте пошту" (`/register/check-email`); посилання в листі діє 24 год; кнопка "Надіслати повторно" (`POST /api/auth/resend-verification`)

---

## 🐛 ФАЗА 17 — Критичні баги (production)

### T-1701 — `POST /api/books` → 401 UNAUTHORIZED при створенні книги

**Сторінка**: `/dashboard/books/new`
**Помилка**: `{"error":"Invalid or missing token","code":"UNAUTHORIZED"}`
**Причина**: клієнтський компонент `BookWizard` робить `fetch('/api/books', ...)` без заголовка `Authorization: Bearer <apiToken>`. Токен є в сесії (`session.user.apiToken`), але не передається в запит.
**Що виправити**:
- [x] **T-1701** У `BookWizard` та всіх `fetch` до `/api/books` — передавати `Authorization: Bearer ${session.apiToken}` (взяти через `useSession()` або `getSession()` на клієнті / `auth()` на сервері)

### T-1702 — `GET/PATCH /api/users/me` + `POST /api/users/me/avatar` → 401/404

**Сторінка**: `/dashboard/settings` (завантаження фото профілю)
**Помилки**:
- `GET /api/users/me` → `{"error":"Invalid or missing token","code":"UNAUTHORIZED"}` — не передається токен
- `PATCH /api/users/me` → `{"error":"Invalid or missing token","code":"UNAUTHORIZED"}` — не передається токен
- `GET /api/users/me/avatar` → 404 Not Found — клієнт робить GET замість POST; маршрут `POST /api/users/me/avatar` існує, GET — ні
**Що виправити**:
- [x] **T-1702a** У компонентах `AvatarUploader` та `settings/page.tsx` — додати `Authorization: Bearer ${apiToken}` до всіх запитів на `/api/users/me` і `/api/users/me/avatar`
- [x] **T-1702b** Перевірити `AvatarUploader` — переконатись що аватар відправляється через `POST`, а не `GET`

### Загальне рішення для T-1701 + T-1702

Використовувати хук `useApi` або централізований `fetchWithAuth` що автоматично читає `apiToken` з сесії і додає заголовок:
```typescript
// hooks/useApi.ts
const session = useSession();
const token = (session.data?.user as any)?.apiToken;
headers: { Authorization: `Bearer ${token}` }
```

### T-1703 — 500 Internal Server Error на `/api/books`, `/api/users/me`, `/api/users/me/avatar`

**Помилки** (після того як авторизація буде виправлена — T-1701/T-1702):
```
/api/books             → 500
/api/users/me          → 500
/api/users/me/avatar   → 500
```
**Діагностика**: перевірити `docker logs knyha-api --tail=100` — 500 означає виняток на сервері (Prisma, MinIO, або некоректне тіло запиту).
- [x] **T-1703** Дослідити логи API при кожному запиті; виправити серверні помилки (найімовірніше: Prisma query crash або відсутній MinIO bucket)

### T-1704 — Авто-генерація slug з імені автора на `/dashboard/settings`

**Поведінка**: при введенні імені автора в полі "Ім'я" — поле slug автоматично заповнюється транслітерованим значенням (uk → latin), в нижньому регістрі, пробіли → `-`.
**Приклад**: `Іван Франко` → `ivan-franko`
- [x] **T-1704** На `settings/page.tsx` — через `watch('name')` (React Hook Form) генерувати slug: транслітерація (бібліотека `transliteration` або власна uk→latin таблиця) + `.toLowerCase().replace(/\s+/g, '-').replace(/[^a-z0-9-]/g, '')`; slug залишається редагованим вручну

---

## 📜 ФАЗА 12 — Юридика на сайті

- [x] **T-1201** Сторінка `/author-agreement` — повний текст договору + accept bar
- [x] **T-1202** Зберігати `contractAcceptedAt` + IP при акцепті
- [x] **T-1203** Сторінка `/terms` — умови використання
- [x] **T-1204** Сторінка `/privacy` — політика конфіденційності
- [x] **T-1205** Cookie banner (GDPR)

---

## 🚀 ФАЗА 13 — Деплой

- [x] **T-1301** `docker-compose.prod.yml`
- [x] **T-1302** ~~Nginx конфіг~~ → Caddy (зовнішній, `dddcore_default` мережа) — Caddy reverse proxy `ulit.render.ua` → `knyha-web:3000`; SSL автоматично через Let's Encrypt
- [x] **T-1303** SSL — автоматично через Caddy/Let's Encrypt
- [x] **T-1304** GitHub Actions `deploy.yml` → SSH
- [x] **T-1305** `backup-db.sh` — щоденний бекап PostgreSQL → MinIO
- [x] **T-1306** Grafana + Prometheus
- [x] **T-1307** Health check `/api/health`
- [x] **T-1308** Rate limiting (fastify-rate-limit)
- [x] **T-1309** Structured logging (Pino)
- [x] **T-1310** Docker log rotation — `json-file` driver з `max-size`/`max-file` для api, web, worker контейнерів
- [x] **T-1311-a** `container_name` для postgres/redis/worker — виправляє `backup-db.sh` та `docker logs` команди; видалено мертвий `infra/nginx/nginx.conf`
- [x] **T-1311-b** Smoke test у `deploy.sh` — після `docker compose up` чекає 15с і робить `curl /api/health`; при failure: виводить `docker logs knyha-api` і завершується з exit 1 (GitHub Actions позначає деплой як failed)
- [x] **T-1312** Pre-deploy backup — у `deploy.sh` перед `docker compose up --build` запускати `backup-db.sh`; гарантує точку відновлення перед кожною міграцією
- [x] **T-1313** Worker health check — додати `healthcheck` до worker сервісу в `docker-compose.prod.yml`; перевіряти Redis ping через `redis-cli`; дозволяє `docker ps` показувати реальний стан worker
- [ ] **T-1314** Deploy failure alert — відкладено; GitHub Actions вже надсилає email при падінні workflow
- [x] **T-1315** Prometheus `/api/metrics` endpoint — додати `prom-client` до Fastify API; Prometheus вже налаштований scrape `api:3001/api/metrics` але endpoint не існує; дасть: RAM, CPU, HTTP latency p95 в Grafana
- [x] **T-1311** Grafana Loki — централізований збір логів з усіх Docker контейнерів; додати `loki` + `promtail` сервіси до `docker-compose.prod.yml`; підключити Loki як data source у Grafana; дашборд "API Errors" з фільтром по рівню `error` та `warn`; перевага: все на VPS, без зовнішніх сервісів, інтегрується з вже наявним Grafana

### Фаза 13b — Resilience (Фаза 2, після MVP стабілізації)

- [ ] **T-1316** Rollback механізм — перед кожним деплоєм тегувати поточний image як `:previous` (`docker tag knyha-api:latest knyha-api:previous`); при smoke test failure — автоматично повернути попередній image без rebuild
- [ ] **T-1317** Staging environment — деплоїти `dev` гілку у другий stack `knyha-staging` на тому ж VPS (окремі порти/домен); дозволяє тестувати на prod-подібному середовищі до merge в main
- [ ] **T-1318** Managed PostgreSQL — перенести БД з Docker контейнера на Hetzner Managed Database або standalone PostgreSQL як systemd service; усуває ризик втрати даних при Docker daemon crash
- [ ] **T-1319** Hetzner Object Storage замість MinIO — S3-сумісний, ~5€/міс, безкоштовний трафік між VPS та Storage в одному datacenter; прибирає MinIO контейнер та потребу в його backup

---

## 🧪 ФАЗА 14 — Тестування

- [x] **T-1401** Unit: `isbn.service.ts` (Vitest)
- [x] **T-1402** Unit: Zod схеми
- [x] **T-1403** Integration: auth endpoints
- [x] **T-1404** E2E: реєстрація → книга → публікація (Playwright)
- [x] **T-1405** E2E: магазин → купівля → отримання файлу

---

## 💸 ФАЗА 15 — Виплати авторам (Payout System)

> Автор накопичує баланс з продажів → запитує виплату → гроші надходять на картку/рахунок.
> Платформа утримує комісію (налаштовується через env `PLATFORM_FEE_PERCENT`).

### Фаза 15a — Базова інфраструктура

- [ ] **T-1501** `PLATFORM_FEE_PERCENT` в env (default: 25%) — додати до `.env.example`
- [ ] **T-1502** Prisma: модель `PayoutRequest` (id, authorId, amount, status, bankDetails, requestedAt, paidAt)
- [ ] **T-1503** Prisma: поля в `User` — `iban`, `cardNumber`, `taxId` (РНОКПП), `isFop` (ФОП чи фізособа)
- [ ] **T-1504** Prisma міграція
- [ ] **T-1505** `payout.service.ts` — розрахунок балансу автора (сума роялті мінус виплачені)

### Фаза 15b — API

- [ ] **T-1506** `GET /api/payouts/balance` — поточний баланс + список транзакцій
- [ ] **T-1507** `PATCH /api/users/me/bank-details` — автор зберігає IBAN / картку / РНОКПП
- [ ] **T-1508** `POST /api/payouts/request` — запит виплати (мін. поріг 500 UAH, hold 7 днів)
- [ ] **T-1509** `GET /api/payouts/my` — список виплат автора зі статусами
- [ ] **T-1510** `POST /api/admin/payouts/:id/approve` — адмін схвалює → ініціює переказ
- [ ] **T-1511** `POST /api/admin/payouts/:id/reject` — відхилити з причиною

### Фаза 15c — UI автора

- [ ] **T-1512** Сторінка `/dashboard/payouts` — баланс + кнопка «Вивести кошти»
- [ ] **T-1513** Форма банківських реквізитів (IBAN або номер картки + РНОКПП + тип ФОП/фізособа)
- [ ] **T-1514** Список виплат з статусами (PENDING / APPROVED / PAID / REJECTED)
- [ ] **T-1515** Email автору після виплати (сума + дата)

### Фаза 15d — Автоматизація (Фаза 2)

- [ ] **T-1516** LiqPay Mass Payments (B2P API) — автоматичний переказ на картку Privat24
- [ ] **T-1517** WayForPay Settlement API — автоматичний переказ на будь-яку картку
- [ ] **T-1518** IBAN bank transfer через SWIFT/SEPA (для ФОП)
- [ ] **T-1519** Звіт для бухгалтерії — CSV виплат за місяць (ПІБ, РНОКПП, сума, дата)
- [ ] **T-1520** Адмін: `/admin/payouts` — черга очікуючих виплат + масове схвалення

---

## ✏️ ФАЗА 18 — Редактор верстки книги (Page Layout Editor)

> Після конвертації DOCX автор може переглянути кожну сторінку і внести правки:
> додати зображення, скоригувати розміщення тексту, відредагувати біо/фото.
> Реалізується поверх PNG-рендерів сторінок з накладеним Fabric.js canvas.
> _Не плутати з **T-1939** (Фаза 19p) — це структурований текстовий редактор самого рукопису (TipTap/ProseMirror); ця фаза — Fabric.js-анотації (зображення/текстові блоки) поверх готових PNG-сторінок. Різне призначення, обидва актуальні._

### 18a — Перегляд і анотації

- [ ] **T-1801** Режим "редагування сторінки" у Book Viewer — перемикач перегляд/редагування
- [ ] **T-1802** Fabric.js canvas поверх PNG-рендера сторінки (прозорий шар, точна прив'язка по розміру)
- [ ] **T-1803** Інструмент "Додати зображення" — завантажити фото і розмістити на сторінці з масштабуванням і позиціонуванням
- [ ] **T-1804** Інструмент "Текстовий блок" — додати текстовий блок поверх сторінки (шрифт, розмір, колір, прозорість)
- [ ] **T-1805** Інструмент "Геометричні фігури" — прямокутник, лінія (розділювачі, підкреслення)
- [ ] **T-1806** Збереження анотацій: `POST /api/books/:id/pages/:page/annotations` — JSON-стан Fabric.js canvas; зберігати в MinIO `private/pages/BOOKID/page-NNN-annotations.json`

### 18b — Фінальна генерація PDF з анотаціями

- [ ] **T-1807** Job `merge-page-annotations` у worker — для кожної сторінки з анотаціями: рендерити Fabric.js canvas у PNG (через `node-canvas` або `puppeteer`), накласти поверх вихідного PNG сторінки за допомогою Sharp
- [ ] **T-1808** Job `rebuild-pdf-from-pages` — Ghostscript збирає всі сторінки (модифіковані + незмінені) назад у PDF: `gs -dBATCH -dNOPAUSE -sDEVICE=pdfwrite -sOutputFile=final.pdf page-*.png`
- [ ] **T-1809** Після rebuild — перегенерувати EPUB, FB2, MOBI з нового PDF (або зберігати оригінальний DOCX незмінним і застосовувати анотації тільки до PDF-версії)
- [ ] **T-1810** Кнопка "Зберегти та перегенерувати" — запускає job і показує прогрес

### 18c — Редагування біо та фото автора

- [ ] **T-1811** На задній обкладинці (Back Cover) у Book Viewer — кнопка "Редагувати" → відкриває форму: фото, ім'я, біо (max 500 символів)
- [ ] **T-1812** `PATCH /api/users/me` — оновлення bio і avatarUrl прямо з Book Viewer без переходу до налаштувань
- [ ] **T-1813** Реал-тайм оновлення задньої обкладинки після збереження

### 18d — Шаблони сторінок (Фаза 2)

- [ ] **T-1814** Бібліотека шаблонів для типових сторінок: титульний лист, сторінка з присвятою, сторінка з цитатою, біографічна сторінка
- [ ] **T-1815** Застосування шаблону до вибраної сторінки — замінює анотації шаблонним Fabric.js JSON

---

## 🔍 ФАЗА 19 [RIDERO] — UX/UI покращення на основі референсу Ridero

> Тег `[RIDERO]` — всі задачі цієї фази отримані з аналізу 99 скріншотів та HTML-сторінок Ridero.
> Реалізовувати тільки після завершення більш пріоритетних фаз.
> Аналіз збережених сторінок Ridero (`docs/ulit-reference/`) — тільки як візуальний UX-референс.
> Код, стилі та розмітку звідти НЕ копіювати (правило CLAUDE.md п.13).
> Реалізовувати власними компонентами відповідно до нашого стеку.

### 19a — Метадані публікації (Publication.html → наш wizard)

> Готовий деталізований вайрфрейм для цієї форми: Figma `nOXLPYS4l4mxOkCINYdQdT` node `14:418` ("Dashboard / Метадані книги") — Назва / Підзаголовок / Опис з лічильником / Автор (ім'я, прізвище, по-батькові, псевдонім, фото, "Додати співавтора") / Біографія з лічильником / "Над книгою працювали" ("Додати учасника") / Жанри-чипси / Вікові обмеження. Вайрфрейм готовий, домальовувати нічого не треба.

- [x] **T-1901** Вікові обмеження на формі метаданих — select: `0+`, `6+`, `12+`, `16+`, `18+`; зберігати `ageRating` в БД (`Book` model); відображати у магазині (беджик поруч з жанром)
- [ ] **T-1902** Кількість символів опису в реальному часі — лічильник під полем Annotation/Description: мін. 120, макс. 500 символів; колір індикатора (сіро → зелено → червоно при перевищенні). _Частково є: `book/[id]/page.tsx` вже показує `{descValue.length}/5000` з amber-попередженням під 120 символів — бракує max. 500 і червоного стану при перевищенні._
- [x] **T-1903** Підзаголовок книги — `subtitle String?` у Prisma + UI; відображається під назвою в магазині. _На обкладинці (Fabric.js) — не додано, окрема робота в `CoverDesigner`._
- [x] **T-1904** Дискоклейм про ШІ — чекбокс "Текст (або обкладинку) частково/повністю створено за допомогою ШІ" з опційним пояснювальним полем; `aiGenerated Boolean @default(false)` + `aiGeneratedNote String?` у Book; мітка "🤖 Створено за допомогою ШІ" в магазині
- [ ] **T-1905** Валідація полів публікації з чіткими повідомленнями — "Опис має містити від 120 до 500 символів", "Вкажіть жанр перед публікацією" тощо; показувати inline під кожним полем

### 19b — Крок-за-кроком публікація (Tab-based workflow)

- [ ] **T-1906** Горизонтальні вкладки на сторінці книги — `Рукопис | Обкладинка | Метадані | Публікація`; стан кожної вкладки: ✓ done / ○ pending / ⚠ needs attention; URL-фрагмент `?tab=cover`. _Замінено новим підходом — див. **T-1933**: замість горизонтальних вкладок зверху сторінки книги тепер ліве акордеон-меню зі списком усіх книг автора._
- [ ] **T-1907** Індикатор прогресу готовності книги (%) — підраховується автоматично з кількості заповнених обов'язкових полів; показувати на картці книги в `/dashboard/books`
- [ ] **T-1908** Застереження при виборі жанру — показувати підказку "Книги певних жанрів проходять розширену перевірку" для чутливих категорій (18+, політика)

### 19c — Dashboard автора (Мои книги.html)

- [ ] **T-1909** Статистика продажів в кабінеті — `/dashboard/stats`: графік продажів по місяцях (Chart.js або Recharts), топ-книги по доходу, кількість завантажень кожного формату
- [ ] **T-1910** Дзвіночок сповіщень у хедері — іконка з badge-лічильником непрочитаних; тип подій: книга схвалена/відхилена, виплата роялті, нова покупка; `Notification` model у Prisma
- [ ] **T-1911** Бонусна система для авторів — накопичувальні бонуси за публікації, продажі, відгуки; `BonusTransaction` model; відображати баланс бонусів у хедері
- [ ] **T-1912** Статус договору в сайдбарі — показувати "Договір підписано ✓" або "Договір не підписано ⚠" з посиланням на `/author-agreement`

### 19d — Редактор обкладинки (cover_own.html)

- [x] **T-1913** Синхронізація обкладинки з метаданими — `CoverDesignerCanvas.tsx` вже передає живі `bookTitle`/`bookAuthor` в `Template.apply()` і при першому відкритті, і при виборі будь-якого шаблону (`applyTemplate`), тож текст на шаблоні завжди відповідає поточним метаданим книги. _Відрізняється від опису: синхронізація автоматична завжди (без чекбоксу) — після ручного редагування тексту користувачем повторний вибір того ж шаблону перезапише правки, що логічно і не потребує окремого перемикача._
- [ ] **T-1914** Вибір формату обкладинки — перемикач: Електронна книга (стандарт 6×9) / Тверда обкладинка (з корінцем і задньою сторінкою) / М'яка обкладинка; кожен формат — окремі пропорції canvas. **Заблоковано** 2026-08-08: `DISPLAY_W`/`DISPLAY_H` (350×525) — модульні константи, на які напряму замкнені всі 6 шаблонів у `CoverDesignerCanvas.tsx` (кожен `Template.apply` використовує їх безпосередньо, не як параметр). Динамічний розмір canvas вимагає рефакторингу шаблонів — ризик зламати робочі undo/redo, snap-to-angle, шаблони (T-508-511). Додавати перемикач без реальної зміни пропорцій — робити фейкову кнопку, свідомо не робив.
- [x] **T-1915** Дискоклейм про ШІ-обкладинку — покрито полем `Book.aiGenerated` з T-1904: чекбокс на формі метаданих уже явно каже "Текст **(або обкладинку)** частково/повністю створено за допомогою ШІ". Окремого дубльованого чекбоксу всередині `CoverDesignerCanvas.tsx` не додавав — той самий прапорець на рівні книги, а не окремий для тексту й окремий для обкладинки.

### 19e — Preview (preview.html / preview_3d.html)

- [ ] **T-1916** 3D-превью обкладинки — CSS 3D transform (perspective + rotateY) для відображення книги у вигляді об'ємного об'єкта; використовувати на сторінці книги в магазині та в кабінеті автора після публікації
- [ ] **T-1917** Кнопка "Завантажити файли" в кабінеті автора — після конвертації показувати dropdown зі всіма доступними форматами: PDF (онлайн), PDF (друк), EPUB, FB2, MOBI; кожен — signed MinIO URL 48 год

### 19f — Магазин та лендінг

- [ ] **T-1918** Секція відгуків авторів на головній сторінці магазину — карусель з 3–5 цитатами від реальних авторів (ім'я, фото, книга); статичний контент в коді або CMS-like JSON у MinIO
- [ ] **T-1919** Блок "Чому Ulit" на головній — порівняльна таблиця переваг: швидко, безкоштовно, ISBN, 70% роялті, EPUB+FB2+MOBI одночасно
- [ ] **T-1920** Сторінка послуг `/services` — перелік додаткових послуг (коректура, редактура, верстка, дизайн обкладинки від дизайнера); кожна послуга — ціна, опис, CTA "Замовити"; поки що статична, без оплати

### 19g — "Що далі після публікації?" (скріншоти 53–55)

- [ ] **T-1921** Сторінка "Книга опублікована — Що далі?" — після успішної публікації переадресовувати на `/dashboard/books/:id/published`; вітальне повідомлення + поле з посиланням на книгу (кнопка "Скопіювати"); жовтий фон, сітка 4×3 карток з рекомендованими діями: поділитись у соцмережах, додати посилання в email-підпис, замовити тираж, організувати авторський захід, написати прес-реліз тощо; чекбокс "Вже зроблено" на кожній картці (зберігати у localStorage)

### 19h — Статистика та роялті (скріншоти 51–52, 70–72)

- [ ] **T-1922** Детальна статистика по платформах — `/dashboard/stats`: таблиця з рядками по магазинах (Ulit, ЛітРес, Amazon…), колонками "Продано" та "Зароблено"; collapsible рядок "За весь час" + по місяцях (розгортати/згортати); вкладки "В магазині Ulit" / "Усі платформи"
- [ ] **T-1923** Сторінка роялті з балансом — `/dashboard/royalties`: картка "Сума до виплати" (виділена сума + пояснення терміну) + кнопка "Запросити виплату" (яка веде на верифікацію або форму); блок "Статистика виплат": Зароблено за весь час / Виведено / Залишок; перша ітерація — заглушка без реального виведення

### 19i — Дистрибуція та повторна публікація (скріншоти 47–49, 94–96)

- [ ] **T-1924** Матриця дистрибуції при публікації (розширена версія T-606) — таблиця магазинів: чекбокс + поле роялті (%), розрахункова ціна в магазині (автоматично), умови розміщення (текст); секції "Продаж е-книги" та "Продаж друкованої книги" окремо; `distributionChannels Json?` в Book → замінює `String[]` з T-606
- [ ] **T-1925** Попередження при повторній публікації — якщо книга вже PUBLISHED і автор вносить зміни, показувати банер: "Безкоштовна модерація доступна 1 раз на 90 днів. Зміни: Текст, Обкладинка. Що зміниться: [таблиця магазинів із датою синхронізації]"; кнопки "Опублікувати зміни" і "Зняти з публікації"

### 19j — Обкладинка (скріншоти 98–99)

- [~] **T-1926** Завантаження готової обкладинки — вкладка "Своя обкладинка" в редакторі поруч із "Шаблони": drag-and-drop зона (мін. 915×1270px, 150 DPI); валідація розміру перед завантаженням; для тиражу — окрема кнопка "Задня сторінка обкладинки"; посилання "Завантажити PSD-шаблон" (файл у MinIO public). _Вайрфрейм: Figma `nOXLPYS4l4mxOkCINYdQdT` node `16:1185` ("Своя обкладинка" — вкладки Електронна/Мʼяка/Тверда, кнопки "Завантажити ілюстрацію" / "Скачати PSD-шаблон" / AI-генерація)._
  - [x] Вкладка "Своя обкладинка" в `CoverDesignerCanvas.tsx` — drag-and-drop/вибір файлу, валідація мін. 915×1270px (з точним повідомленням про фактичний розмір), при успіху повністю замінює канву завантаженим зображенням; збереження — тією ж кнопкою "Зберегти обкладинку", що й шаблони
  - [x] "Задня сторінка обкладинки" — реалізовано 2026-08-08: `Book.backCoverUrl` (нове поле), `POST /api/books/:id/upload-back-cover`, новий компонент `BackCoverEditor.tsx` (окрема Fabric.js канва: колір фону, власне зображення, текст — без шаблонів-тайтлів); підключено на `/dashboard/books/:id/cover` через перемикач "Передня обкладинка / Задня сторінка" (замість кнопки всередині `CoverDesignerCanvas.tsx` — чистіше розділяти дві незалежні канви на рівні сторінки, ніж запихати другу канву в той самий toolbar). Товщина корінця — орієнтовний розрахунок `pageCount × 0.1мм`, лише інформативний напис, нікуди не передається.
  - [~] Посилання "Завантажити PSD-шаблон" — потрібні 2 файли (м'яка обкладинка + тверда обкладинка з полями bleed/safe zone для поліграфії), готує Анатолій. Кнопки додані у вкладку "Своя обкладинка" (`CoverDesignerCanvas.tsx`), навмисно **неактивні** (`disabled`) поки файлів немає. Коли файли будуть готові: завантажити в MinIO як `public/templates/cover-template-softcover.psd` і `public/templates/cover-template-hardcover.psd`, після чого прибрати `disabled` і додати `href` на `publicUrl(...)`.
- [ ] **T-1931** Повний прев'ю друкованої обкладинки — розгорнутий wrap-cover (задня сторона + корінець + передня) на всю ширину з горизонтальним zoom-повзунком під зображенням; показувати на `/dashboard/books/:id/cover` після вибору формату "Тверда"/"М'яка обкладинка". _Вайрфрейм: Figma node `14:1007` (zoom in) / `15:1099` (zoom out)._ Тепер **частково розблоковано**: задня сторона (`backCoverUrl`) вже є, корінець і композиція трьох панелей в одне зображення — все ще не реалізовано.

### 19k — Метадані (скріншоти 40–41, 91–92)

> Ці два поля також присутні у вайрфреймі `14:418` (див. 19a) — "Над книгою працювали / Додати учасника" (T-1927) і блок chips "Психологія / Здоров'я / Самовдосконалення / Додати жанр" (T-1928).

- [x] **T-1927** Підтримка співавторів — кнопка "+ Додати" у формі метаданих; `coAuthors Json?` у Book model; відображаються на сторінці книги в магазині. _Спрощено відносно опису: лише ім'я (без прізвища/фото окремими полями, без "У файловому метаданих" — EPUB/FB2 генерація співавторів не оновлена)._
- [ ] **T-1928** Жанри як мульти-вибір chips — замість `genre String?` — `genres String[]` у Prisma (міграція!); UI: поле пошуку + список жанрів → вибрані відображаються як chips (назва + ×); попередження "Переконайтесь, що жанри вибрано правильно"; відображати всі жанри на сторінці книги в магазині. _Свідомо відкладено 2026-08-08: замінює тип існуючого поля `genre`, яке зараз використовується в фільтрах каталогу (`GET /api/store/books?genre=`), картках `BookCard`, чеклісті вимог платформ на `/admin/books/:id/distribute`; потребує окремого проходу по всіх цих місцях, а не просто розширення форми._

### 19l — Перегляд книги (скріншоти 30–38, 86–90)

- [x] **T-1929** Три вкладки у viewer — у `/dashboard/books/:id/preview` додати tabs: "Обкладинка" (front cover image) / "Кольорова книга" (PDF з кольором) / "Чорно-біла книга" (ч/б рендер); навігатор сторінок у вигляді повзунка "1 ←●→ 74"; кнопки "3D" (CSS perspective + rotateY) та "PDF" (signed URL на pdfUrl); поточний viewer → вкладка "Кольорова книга". _Вайрфрейм: Figma `nOXLPYS4l4mxOkCINYdQdT` nodes `14:657`/`14:841`/`14:924`._
  - [x] `BookViewer.tsx` переписаний: вбудований у сторінку (не full-screen модал), розворот сторінок (ліва+права), повзунок-навігатор замість стрічки мініатюр
  - [x] Три вкладки "Обкладинка / Кольорова книга / Чорно-біла книга" — перемикають джерело сторінок (`pages` vs `pagesBw`); вкладка "Обкладинка" — окремий недвосторінковий вигляд
  - [x] Чорно-білий рендер сторінок — `generate-page-thumbnails.ts` тепер конвертує кожен PNG через `sharp().grayscale()` і завантажує паралельно в `public/pages-bw/{bookId}/`; `GET /api/books/:id/pages` повертає обидва набори (`pages`, `pagesBw`)
  - [x] "3D"/"PDF" перемикач — "3D" статичний perspective/rotateY тілт (не анімація перегортання), "PDF" відкриває signed `pdfUrl` у новій вкладці
  - [ ] Справжня анімація перегортання сторінок (page-flip) — залишається статичним тілтом, не пріоритет зараз

### 19m — Завантаження файлів автором (скріншоти 50, 97)

- [ ] **T-1930** Сторінка завантаження форматів — `/dashboard/books/:id?tab=download`: для кожного готового формату (EPUB 3, FB2, PDF для читання, MOBI) — іконка, коротке пояснення ("EPUB 3 — рекомендовано для iBooks та Kindle", "FB2 — для більшості е-читалок", тощо), кнопка "Завантажити" (signed MinIO URL на 48 год); якщо формат ще не згенерований — показувати статус "Генерується..."

### 19n — Статус публікації та договір (Figma `14:419`/`14:489`)

> Новий екран, знайдений при аналізі вайрфреймів 2026-08-08 — не існував раніше в TASKS.md. Розширює статичний `/author-agreement` (T-1201/T-1202) до per-book покрокового статусу.

- [x] **T-1932** Таймлайн статусу публікації книги — на `/dashboard/books/:id` вертикальний степер із кроками: Книга створена → Надішліть книгу на публікацію → Перевірка завершена → Укладіть договір → Договір виправлено → Повторна перевірка документів → Договір укладено → Публікація у магазинах (з ISBN) → дата оновлення статистики продажів по площадках (Draft2Digital / Amazon KDP / Google Play Books, кожна ✓/✗ окремо). _Реалізовано без e-sign інтеграції — усі 6 проміжних кроків проставляються вручну адміном (дата або порожньо) через новий `Book.publicationTimeline Json?` + `PATCH /api/admin/books/:id/publication-timeline`, секція "Статус публікації та договору" на `/admin/books/:id/distribute`; автор бачить read-only степер у `PublicationTimeline.tsx` на `/dashboard/books/:id`. "Книга створена" і "Публікація у магазинах" — обчислюються автоматично (`createdAt`, `status===PUBLISHED && isbn`), решта — ручні мітки адміна._

### 19o — Лівий сайдбар навігації автора (Figma-огляд 2026-08-08, повторний прохід)

> Анатолій показав повний layout-редизайн кабінету автора: постійний лівий сайдбар зі списком УСІХ книг автора (акордеон — розгорнута лише поточна, з підпунктами), контент праворуч максимально використовує ширину. Підтверджено обсяг: розбити `[id]/page.tsx` на під-сторінки за пунктами сайдбару; сайдбар показує всі книги одразу; застосувати на `/dashboard/books`, `/dashboard/books/:id`, `/cover`, `/preview`. Повторний прохід по 9 фреймах Figma `nOXLPYS4l4mxOkCINYdQdT` підтвердив, що майже все вже покрито задачами T-1901–T-1932 (див. WIREFRAMES нижче) — окрім самого сайдбару і кількох пунктів меню без жодної існуючої фічі за ними.

- [~] **T-1933** Персистентний сайдбар навігації автора — лівий акордеон-список усіх книг (`GET /api/books`); розгорнута книга показує підпункти: Замовити тираж / Обговорення книги / Завантажити файли / Редагувати книгу (Рукопис, Обкладинка, Передперегляд) / Ваша книга у магазинах (Вихідні дані, Публікація у магазинах, Статистика, Знайти нових читачів, Включити акцію на книгу). `[id]/page.tsx` (658 рядків, усі кроки стовпчиком) розбито на окремі routes: `manuscript`, `output-data`, `publish` (+ існуючі `cover`, `preview`, `published`), під новим route group `apps/web/app/dashboard/books/(withSidebar)/`. Активне посилання підсвічується через `usePathname()` (як у `AdminSidebar.tsx`). Пункти без backing-фічі (нижче) — видимі, але `disabled`/"скоро", за прецедентом T-1926 (неактивні PSD-кнопки).
- [ ] **T-1934** AI-генерація обкладинки — кнопка "Створити за допомогою нейромережі" на вкладці "Своя обкладинка" (`CoverDesignerCanvas.tsx`), Figma `16:1185`. MVP без реальної text-to-image генерації: кнопка відкриває форму запиту (короткий опис/побажання) → зберігається як заявка (нова модель `CoverAiRequest`: `id, bookId, prompt, status(REQUESTED/DONE), createdAt`) → адмін бачить чергу заявок, вручну завантажує готовий варіант через існуючий upload-cover ендпоінт. Реальна інтеграція з генератором зображень — окрема майбутня задача.
- [ ] **T-1935** "Замовити тираж" — автор замовляє друковану партію (N примірників, не одну книгу через магазин). Нова модель `PrintOrder`: `id, bookId, authorId, quantity, coverType(SOFTCOVER/HARDCOVER), shippingAddress, status(REQUESTED/QUOTED/PAID/PRINTING/SHIPPED), quotedPrice?, createdAt` — окремо від `Order`/`OrderItem` (ті для покупця в магазині). API: `POST /api/books/:id/print-orders`, `GET/PATCH /api/admin/print-orders`. UI: `/dashboard/books/:id/print-order` — форма (кількість, тип обкладинки, адреса) + статус заявки. Перша ітерація без оплати всередині системи — адмін вручну проставляє ціну й зв'язується з автором (як зараз працює виплата роялті, T-1923).
- [ ] **T-1936** "Обговорення книги" — переписка автор ↔ модератор по конкретній книзі, окремо від разового `moderationNote` при відхиленні. Нова модель `BookComment`: `id, bookId, authorId(userId — автор або адмін), body, createdAt`. API: `GET/POST /api/books/:id/comments` (автор, лише власна книга) + `GET/POST /api/admin/books/:id/comments` (той самий стіл, адмінська сторона). UI: `/dashboard/books/:id/discussion` — простий тред + textarea; дзеркальна вкладка на `/admin/books/:id/distribute`. MVP без real-time (refetch після відправки, без WebSocket).
- [ ] **T-1937** "Знайти нових читачів" — контентна сторінка з практичними діями для промоції книги (без нової моделі даних), перевикористовує ідеї з T-1921 (поділитись у соцмережах, email-підпис, прес-реліз): `/dashboard/books/:id/find-readers` зі списком карток-порад + готові шаблони посилань для шеру. Якщо T-1921 вже реалізовано на момент виконання — переюзати той самий контент/компонент.
- [ ] **T-1938** "Включити акцію на книгу" — тимчасова знижка на книгу. Нові поля `Book.promoPriceEbook Decimal?`, `promoPricePrint Decimal?`, `promoStartsAt DateTime?`, `promoEndsAt DateTime?`. API: `PATCH /api/books/:id/promotion`. UI: `/dashboard/books/:id/promotion` — перемикач + ціни + діапазон дат. Магазин (`GET /api/store/books/:slug` + сторінка книги) показує перекреслену звичайну ціну + акційну, коли `now()` в межах `promoStartsAt..promoEndsAt`.

### 19p — Online-редактор рукопису (Figma `31:149`, вайрфрейм 2026-08-09)

> Анатолій домалював фінальний вайрфрейм для "Рукопис (редагувати)": повноцінний WYSIWYG-редактор у стилі Word, з панеллю "Зміст" (структура документу) зліва і панеллю "Стилі тексту" справа. Це НЕ те саме, що Фаза 18 (Page Layout Editor) — там Fabric.js-анотації поверх PNG-рендерів сторінок (для зображень/фото автора), тут — структурований текстовий редактор самого рукопису. Обидва підходи існують паралельно, різне призначення.

- [~] **T-1939** Online-редактор рукопису — `/dashboard/books/:id/manuscript` (поза `(withSidebar)`, власний "Зміст"-сайдбар замість списку книг). Технології: TipTap/ProseMirror, кастомний `styledParagraph` (9 стилів: Розділ/Глава/Заголовок/Підзаголовок/Звичайний/Епіграф/Цитата/Вірші/Дата-Підпис). Імпорт: `POST .docx upload` → worker job `MANUSCRIPT_IMPORT` (`pandoc -t json` → `pandocToEditorDoc.ts` мапить Pandoc AST у TipTap JSON) → `Book.manuscriptContent Json?`/`manuscriptImportedAt DateTime?`; API `GET/PATCH /api/books/:id/manuscript`. Панель "Зміст" — клікабельний outline (Розділ/Глава/Заголовок/Підзаголовок у порядку документу), toolbar (bold/italic/underline/strike/списки/indent/вирівнювання/undo-redo), автозбереження (debounce 2с). _Обмеження імпорту: "Розділ" (Part), "Епіграф", "Вірші", "Дата/Підпис" не мапляться автоматично з generic .docx — автор дотегує вручну через праву панель після імпорту._ Працює **паралельно** до існуючого docx→LibreOffice→PDF/EPUB/FB2/MOBI пайплайна (той не змінено).
- [ ] **T-1940** Регенерація фінальних форматів з відредагованого рукопису — зараз PDF/EPUB/FB2/MOBI/друк генеруються тільки з оригінального .docx; правки в онлайн-редакторі (`manuscriptContent`) ніде не використовуються для фінальних файлів. Головний наступний крок після T-1939 (потребує JSON→HTML/PDF рендер-пайплайну, окремого від LibreOffice).
- [~] **T-1941** Галерея шаблонів верстки сторінки в редакторі рукопису — права панель, нижня частина (Business Regular / Business Regular Left / Business Elegance, Figma `31:149`, дані витягнуті з растрового скріншоту в Figma — назви/описи точні, міні-прев'ю сторінок намальовані заново CSS-блоками, не оригінальні іконки). Картки клікабельні, з visual-selected станом (`selectedLayout`). **Не реалізовано**: реальне застосування шаблону до друкованої верстки/генерації PDF — прив'язано до T-1940 (немає print-рендер-пайплайну), поки це чисто UI-вибір без ефекту.
- [ ] **T-1942** Розширення toolbar редактора рукопису — sub/superscript, посилання, вставка зображення, таблиця, пошук/заміна. Не реалізовано в MVP (T-1939).
- [x] **T-1943** "Зберегти новий стиль" — кнопка біля активного пункту в панелі "Стилі тексту" (Word-подібне "update style to match selection"): захоплює поточні marks (bold/italic/underline/strike) + вирівнювання виділення, застосовує їх до **всіх** блоків з цим стилем по всьому документу (`applyStyleFormatting` — ProseMirror-транзакція, обхід `doc.descendants`), і запам'ятовує override для майбутніх блоків цього стилю (застосовується автоматично при виборі стилю через `applyStyleToSelection`). Зберігається в новому `Book.manuscriptStyleOverrides Json?` разом з контентом (той самий `PATCH /api/books/:id/manuscript`, розширений полем `styleOverrides`).
- [x] **T-1944** Особисті "Набори стилів" автора — окрема сутність `AuthorStyleSet` (`id, authorId, name, description?, styleOverrides`), не прив'язана до конкретної книги — доступна з будь-якої книги автора. API: `GET/POST/DELETE /api/style-sets`. UI: секція "Мої набори стилів" під вбудованими шаблонами — картки в тому самому форматі, що й 3 вбудовані набори, але **без міні-прев'ю зображення** (за прямою вказівкою); кнопка збереження відкриває інлайн-форму (Назва + короткий опис), клік по збереженому набору застосовує його `styleOverrides` до поточного документа тим самим механізмом, що й T-1943.

### 19q — Відкладено з сесії 2026-08-09 (навігація/дашборд/чекліст)

- [ ] **T-1945** Реальне вивантаження .docx на кроці "Файл" майстра створення книги (`BookWizard.tsx`, крок 1) — зараз стаб-заглушка ("Завантаження DOCX буде доступне в наступному оновленні"), яка нічого не вивантажує і просто пропускає крок. Це причина того, що книга після майстра лишається без рукопису (виглядає як "чернетка без прогресу"). Треба підключити той самий upload-механізм, що вже працює на `/dashboard/books/:id/manuscript` (`DocxUploader` + `POST /api/books/:id/upload-docx`), або прибрати крок і одразу скеровувати автора на сторінку "Рукопис" після створення чернетки.
- [ ] **T-1946** З'ясувати чому автоматичний деплой (`deploy.yml`, `workflow_run` після CI) не запустився для 2 останніх пушів поспіль — `git rev-parse` на VPS показував старий коміт, `docker inspect` підтверджував що контейнер `api` не перезапускався, хоча build і backup-скрипт при ручному запуску відпрацьовували без помилок. Обидва рази задеплоєно вручну через SSH (`bash infra/scripts/deploy.sh`). Перевірити історію запусків GitHub Actions (потрібен `gh auth login` або токен — CLI `gh` не встановлений локально) — можливо CI-джоба сама не проходила, або `workflow_run` тригер не спрацьовував.
- [x] **T-1947** Локально зламаний `pnpm --filter web lint` (`next lint`) — видавав `Parsing error: Cannot read file 'apps/web/apps/web/tsconfig.json'` (подвоєний шлях) для ВСІХ файлів. Причина: `next lint` сам підставляє `tsconfigRootDir = apps/web`, а старий `.eslintrc.json` мав відносний `parserOptions.project: "apps/web/tsconfig.json"` (розрахований на резолв від кореня репо) — шлях подвоювався. Рішення: `.eslintrc.json` → `.eslintrc.js` з `path.join(__dirname, "apps/web/tsconfig.json")` (абсолютний шлях, незалежний від cwd/tsconfigRootDir). Заразом вимкнено базові (не TS-aware) `no-undef`/`no-unused-vars` для `apps/web/**/*.{ts,tsx}` — фолсили на JSX без імпорту `React` та на іменах параметрів у TS-сигнатурах типів; `tsc --noEmit` і так покриває обидва випадки коректно.

### 19r — Публікація змін / контракт / T-1940 (референс Рідеро, сесія 2026-08-09)

> Референс UI взято зі скріншотів Ridero.ru (Figma `nOXLPYS4l4mxOkCINYdQdT`, node `53-1136`/`53-1146`/`53-1150`), детальний опис — `docs/TECHNICAL-DECISIONS.md` розділ "🔁 Референс: республікація змін на Рідеро".

- [x] **T-1948** "Опублікувати із змінами" — реалізовано: `apps/api/src/modules/books/republish.ts` (`POST /api/books/:id/republish`), дозволено повторне завантаження `.docx` для `PUBLISHED` книги (`upload.ts`, стейджиться без конвертації), `RepublishButton.tsx` активний коли `docxUpdatedAt > publishedAt`.
- [x] **T-1949** Перехід на модель Ridero — **повторна модерація замість миттєвої публікації** (рішення прийнято 2026-08-09). Автор тисне "Опублікувати із змінами" → `Book.republishRequestedAt = now`, файли/publishedAt НЕ чіпаються, книга лишається live зі старими файлами. Адмін бачить бейдж "⏳ Зміни на модерації" в `/admin/books` (тільки для `PUBLISHED` книг з `republishRequestedAt`), тисне "✓ Схвалити зміни" (`PATCH /api/admin/books/:id/republish` — регенерує EPUB/PDF/FB2/MOBI, оновлює `publishedAt`, чистить `republishRequestedAt`) або "✕ Відхилити зміни" (`PATCH /api/admin/books/:id/republish/reject` — чистить `republishRequestedAt`, зберігає причину в `moderationNote`, автор може виправити й надіслати знову). **Свідомо не перенесено з Ridero**: rate-limit 90 днів (недоцільний при одному адміні — ручний рев'ю вже є природним лімітом) і чіткий діф-чипи "що змінилось" (Текст/Вихідні дані/Обкладинка) — зараз відстежується лише "рукопис оновлено" (`docxUpdatedAt`), без per-поля діфу. Діф-чипи — залишити як окрему майбутню задачу, якщо знадобиться.
- [x] **T-1950** "Зняти книгу з публікації" (unpublish без видалення) — реалізовано: новий `BookStatus.UNPUBLISHED` + `Book.unpublishedAt`; `POST /api/books/:id/unpublish` (автор, лише для `PUBLISHED`) ставить `status = UNPUBLISHED`, файли/ISBN/`publishedAt` не чіпаються — книга миттєво зникає з магазину Ulit і чекауту (усі `store`/`orders`-запити й так фільтрують строго `status === "PUBLISHED"`); зняття з зовнішніх майданчиків (D2D/KDP/Google) лишається існуючим ручним кроком адміна через `PATCH /api/admin/books/:id/distribution`, як і решта дистрибуції. `POST /api/books/:id/relist` повертає книгу назад у `PUBLISHED` без повторної модерації (контент не змінювався). UI: `UnpublishButton.tsx` (з модалкою підтвердження) поруч з `RepublishButton.tsx`, `RelistButton.tsx` замінює `PublishButton.tsx` поки книга знята. На відміну від `ARCHIVED` — книга лишається серед активних книг автора (`/dashboard/books`), не в архіві.
- [ ] **T-1951** Прогнозна дата відправки на кожен зовнішній магазин ("Литрес: зміни будуть відправлені 01.09.2026") замість поточного бінарного статусу `d2dStatus`/`kdpStatus`/`googleStatus`. Потребує рішення, як саме плануються дати відправки (зараз усе вручну адміном, без розкладу).
- [x] **T-1952** Персоналізований `.docx` договору — при завантаженні на `/dashboard/books/:id/contract` тепер підставляється блок підпису з реальними даними автора (ІПН, паспорт/ФОП, IBAN, ім'я), а не порожній шаблон — аналог того, як Ridero видає вже заповнений PDF-договір після підписання (page 4: ФІО/паспорт/банківські реквізити в блоці підпису).

---

## 🔭 ФАЗА 20 — Observability & Error Tracking

> Автоматичний збір, сповіщення та документування помилок — без ручного SSH і `docker logs`.
> Впроваджувати після стабілізації MVP.

### 20a — Sentry (пріоритет: ВИСОКИЙ)

- [ ] **T-2001** Sentry акаунт + проект (безкоштовний тир: 5k помилок/міс) — `sentry.io`; отримати DSN для api та web
- [ ] **T-2002** `@sentry/node` у `apps/api` — ініціалізація перед Fastify; інтеграція з global error handler (автоматично відправляти всі 5xx з повним stack trace, request context, user id)
- [ ] **T-2003** `@sentry/nextjs` у `apps/web` — `sentry.client.config.ts` + `sentry.server.config.ts`; перехоплення клієнтських JS-помилок та серверних Next.js помилок
- [ ] **T-2004** Alerts у Sentry — email-сповіщення при першій появі нової помилки; Slack/Telegram webhook (опціонально); дайджест нових помилок за добу
- [ ] **T-2005** Source maps upload у CI/CD — щоб stack trace у Sentry показував оригінальний TypeScript, а не скомпільований JS; додати крок `sentry-cli sourcemaps upload` до `deploy.yml`
- [ ] **T-2006** Performance Monitoring у Sentry — відстежувати повільні API endpoints (p95 latency); встановити threshold 500ms → alert; корисно для виявлення повільних Prisma queries

### 20b — Grafana Loki (впроваджується разом з T-1311)

- [ ] **T-2007** Loki data source у Grafana — після T-1311 підключити Loki; створити дашборд "Application Logs" з панелями: rate помилок per minute, останні 50 `error`-рядків, кількість 5xx відповідей
- [ ] **T-2008** Alert rule у Grafana — якщо `count(level="error") > 5 за 5 хвилин` → email сповіщення адміну

---

## 🎨 WIREFRAMES — на черзі (Анатолій)

> Зробити wireframe → повідомити Claude → Claude бере в реалізацію.

- [x] **W-01** Book Viewer `/dashboard/books/:id/preview` — вайрфрейм готовий через Figma `nOXLPYS4l4mxOkCINYdQdT` (сторінка "Book Viewer"), деталізовано у nodes `14:657`/`14:841`/`14:924` → реалізація описана в **T-1929**. _Початкові 6 порожніх ескіз-фреймів `4:2`–`4:7` (розворот/thumbnail strip/навігація/skeleton/обкладинка×2) застарілі й дублюють `14:657` серію — пропущені за рішенням Анатолія, домальовувати не треба._
- [ ] **W-02** Виплати автора `/dashboard/payouts` — блок балансу, форма реквізитів (IBAN/картка/РНОКПП), кнопка запиту, список виплат зі статусами
- [ ] **W-03** Статистика продажів `/dashboard/stats` — графік по місяцях, таблиця по платформах (collapsible), вкладки, топ-книги, мобільна версія

> **Повний аналіз Figma-файлу `nOXLPYS4l4mxOkCINYdQdT`** (2026-08-08): окрім W-01, файл містить готові детальні вайрфрейми для:
> - `14:419`/`14:489` — таймлайн статусу публікації/договору → **T-1932** (нова, Фаза 19n)
> - `14:1007`/`15:1099` — розгорнутий wrap-cover з zoom-повзунком → **T-1931** (Фаза 19j)
> - `16:1185` — вкладка "Своя обкладинка" → уточнює **T-1914**/**T-1926**
> - `14:418` — повна форма метаданих → уточнює **T-1901–T-1903, T-1927, T-1928** (Фаза 19a/19k)
> - `16:1272` — це реальний скріншот редактора **Ridero** (не наш мокап), вбудований як референс far-future бачення вбудованого WYSIWYG-редактора рукопису; не відповідає жодній існуючій задачі, не актуально зараз (найближче — Фаза 18, але там інша архітектура: Fabric.js-анотації поверх PNG, не текстовий редактор). Дій не потребує.
> - node `7:11` — накладення тексту "Біографія *" на "Рукопис (.docx)" підтверджено як помилка макетування (не продуктова вимога) — ігнорується.

> **Повторний прохід 2026-08-09** (Анатолій показав 9 фреймів з поясненнями, кожен окрім `16:1272` має лівий сайдбар): підтверджено, що це новий layout для всього кабінету автора, а не окрема сторінка. Новий сайдбар + розбиття `[id]/page.tsx` на під-сторінки → **T-1933** (Фаза 19o). Пункти сайдбару без існуючої фічі задокументовано окремими задачами: AI-обкладинка → **T-1934**, "Замовити тираж" → **T-1935**, "Обговорення книги" → **T-1936**, "Знайти нових читачів" → **T-1937**, "Включити акцію на книгу" → **T-1938**. Стара задача **T-1906** (горизонтальні вкладки) позначена застарілою — замінена на T-1933.
>
> **`16:1272` доопрацьовано, з'явився `31:149`** (2026-08-09): `16:1272` — це перехід із сайдбару в редагування (меню); `31:149` — фінальний вайрфрейм самої сторінки редагування рукопису (WYSIWYG, "Зміст"-панель, "Стилі тексту"). Реалізація → **T-1939** (нова, Фаза 19p).

---

## 📊 MVP Scope (мінімально робочий продукт)

```
Фази 0–3 (повністю)
+ T-401..T-413  (конвертація)
+ T-501..T-507  (обкладинка)
+ T-601..T-605  (KU вибір)
+ T-701..T-706  (ISBN + публікація)
+ T-801..T-810  (магазин)
+ T-901..T-908  (оплата LiqPay)
+ T-1101..T-1119 (адмін-панель — базова)
+ T-1201..T-1205 (юридика)
+ T-1301..T-1309 (деплой)
```

Після MVP: Stripe, EPUB reader, e2e тести, автоматизація D2D.
