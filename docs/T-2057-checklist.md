# ТЗ: єдиний рендер книги (T-2057) + чекліст реалізації/тесту

> Створено 2026-08-17. Позначати `[x]` по факту виконання, дописувати під пунктом "Реалізовано:" — як саме. Не позначати `[x]` без підпису.

## 0. Принцип

**Передперегляд = сама генерація друкованого PDF.** Немає окремого "показу" — кнопка "Передперегляд" запускає той самий рендер, що йде в друк, і показує його результат. Автор бачить не приблизну картинку, а реальний файл, який піде до друкарні.

## 0.1. КРИТИЧНО: автор сам формує макет — конвертер не має його порушувати

Автор готує `.docx` сам, у форматі A5, і сам розставляє "Зміст" із номерами сторінок, що відповідають ЙОГО власній розбивці на сторінки (як Word/LibreOffice порахували в його файлі). З цього прямо випливає:

- Конвертер **не має права**: переносити/переформатовувати абзаци, змінювати розриви сторінок, зсувати чи змінювати розмір/позицію зображень — жодних відхилень макету відносно оригінального `.docx` автора.
- Якщо конвертер це порушить — пагінація зміниться, і номери сторінок в авторському "Зміст" стануть невірними. Покупець отримає книгу зі зламаним змістом.

Це прямо конфліктує з рендером `manuscriptContent` через WeasyPrint/Puppeteer із власними CSS `@page`-правилами (розділ 3) — той підхід ре-флоу'ить контент за нашими правилами (наші шрифти/поля/розриви), що майже гарантовано дає ІНШУ пагінацію, ніж оригінальний Word-файл автора, і ламає його "Зміст". Два можливі напрями розглядались:

- **(А) Пряма конвертація оригінального `.docx`** (те, що вже й так робить сьогоднішній `generate-pdf-print.ts` через LibreOffice) — зберігає пагінацію й "Зміст" автора 1:1. Ціна: онлайн-редактор (`manuscriptContent`) не може напряму впливати на друкований файл без повторного завантаження нового `.docx`.
- **(Б) Рендер через `manuscriptContent`+WeasyPrint/Puppeteer** — дає живий передперегляд онлайн-правок, але НЕ гарантує ту саму пагінацію, що в оригінальному Word-файлі.

**РІШЕННЯ (Анатолій, 2026-08-17)**: (Б) з жорсткою вимогою вірності на обох межах конвертації — docx→редактор без втрат вмісту (жоден абзац/зображення/форматування не губиться), і редактор→PDF 1:1 (той самий рушій рендерить і передперегляд, і друк — розділ 0, не наближення). **Прийнятий і задокументований компроміс**: це НЕ відтворює точну пагінацію оригінального Word-файлу автора (жоден інший рушій рендеру не рахує перенос рядків/сторінок так само, як Word — це фізичне обмеження, не недолік реалізації). Якщо автор написав власний "Зміст" з номерами сторінок у своєму `.docx`, ці номери не гарантовано лишаться вірними після проходження через наш рендер. Оскільки авто-TOC теж прибрано зі scope (розділ 2), питання "як показати коректний Зміст" лишається відкритим на майбутнє — не блокує старт коду зараз.

## 1. Контракт рендера

```
renderManuscriptPdf(input) -> output

input:
  manuscriptContent: Json        // Book.manuscriptContent, TipTap doc
  manuscriptStyleOverrides: Json // Book.manuscriptStyleOverrides
  widthMm: number                // Book.printWidthMm ?? PRINT_TRIM_SIZE_MM.widthMm (жанр-залежно, T-2059/T-2060)
  heightMm: number               // Book.printHeightMm ?? PRINT_TRIM_SIZE_MM.heightMm
  coverUrl?: string

output:
  pdfBuffer: Buffer
  pageCount: number              // реальна кількість сторінок готового PDF
```

Чиста функція — без побічних ефектів, без запису в БД. Різні job'и огортають її по-різному (друк додає CMYK-крок, передперегляд — ні, або взагалі показує той самий print-PDF без окремого кроку).

## 2. Структура документа

| # | Елемент | Сторінка | Опційність |
|---|---|---|---|
| 1 | Порожній форзац | verso | завжди (службова, зсуває титул на recto) |
| 2 | Титул (автор, назва, підзаголовок, видавничий імпринт) | recto | обов'язково |
| 3 | Зворот титулу (УДК/ББК, бібліографічний опис, анотація, вікове обмеження, ISBN, ©) | verso | обов'язково |
| 4 | Присвята | recto | опційно — тільки якщо автор додав |

Тіло: розділ (chapter) завжди з recto; секція/заголовок/підзаголовок нижчих рівнів розрив НЕ форсують; епіграф завжди сам на сторінці; running header зверху; номер сторінки знизу по центру.

**Зі scope T-2057 прибрано (поки що): автоматичний Зміст (TOC).** Причина: якщо автор уже написав власну сторінку "Зміст" у завантаженому `.docx` (типово для самвидаву), код зараз її не розпізнає й не видаляє (`extractOutlineFromDoc` шукає лише героям-стилізовані абзаци, авторський "Зміст" лишається звичайним текстом у тілі) — без окремого рішення це дає два "Зміст" підряд, один з яких завжди бреше про номери сторінок. Плюс невирішене питання позиції (початок vs кінець книги, конвенція Ridero) — **тепер із реальним протиріччям між джерелами**: KDP офіційно вимагає Зміст ближче до початку книги для е-книг (`docs/kdp-publishing-guide.md`, розділ 6), тоді як слов'янська видавнича конвенція (`docs/book-construction-glossary.md`) типово кладе Зміст у кінець. Якщо дбаємо про сумісність експорту з KDP — "на початку" вже не просто стилістичний вибір. Клієнтська TOC-логіка з сесії 2026-08-16 (`extractOutlineFromDoc`/`buildTocContent`/`tocEntry.ts`) лишається в коді як є, не видаляється — просто не переноситься в новий серверний рендер цим ТЗ.

## 3. Технічний рушій рендера

Puppeteer (Chromium) vs WeasyPrint (Python) — критерій: підтримка `break-before: recto` (розділ з правої сторінки нативно, без ручної логіки парності). WeasyPrint підтримує нативно, Chromium — ні (довелось би переносити ручну парність-евристику на сервер).

## 4. QA-інструмент (тимчасовий)

Додати в праву панель передперегляду кнопку **"Завантажити PDF"** — посилання напряму на файл, який показує передперегляд. Мета: дати змогу звірити байт-в-байт, що показаний передперегляд і реальний друкований документ — один і той самий файл. Прибрати кнопку, коли T-2057 підтверджено живим тестом.

---

## Чекліст реалізації T-2057

- [x] Архітектурне рішення (А)/(Б) — **вирішено: (Б)**, docx→редактор без втрат + редактор→PDF 1:1 (єдиний рушій). Компроміс щодо авторської пагінації/Зміст задокументовано в розділі 0.1, не блокує далі.
- [x] Тест вірності імпорту — **FAIL, задокументовано, не виправлено**. Досліджено `apps/worker/src/lib/pandocToEditorDoc.ts` (Pandoc `docx -t json` → TipTap JSON) і `apps/worker/src/lib/docxImageAlignment.ts`. Імпортер свідомо lossy (за власними коментарями коду — "author fixes up the rest"), конкретні втрати:
  - **Таблиці** — повністю відкидаються, немає вузла-таблиці в схемі редактора взагалі.
  - **Виноски** — повністю відкидаються, немає підтримки в схемі.
  - **Розриви сторінок Word** — не імпортуються (тільки ручна кнопка в редакторі створює `pageBreak`-вузол).
  - **Кастомні стилі** (Розділ/Епіграф/Вірш/Підпис) — ніколи не визначаються автоматично з докс-форматування; усе стає `normal`/`section`/`heading`/`subheading`, автор має вручну перепризначити після імпорту.
  - **Багатоабзацні цитати** — сплющуються в один абзац.
  - Будь-який нерозпізнаний Pandoc block/inline-тип (математика, raw-блоки, code-блоки, definition lists) — тихо відкидається (`default: return []`), без сигналу автору.
  - **Вирівнювання зображень** — деградує до "по центру" для ВСІХ зображень одразу, якщо підрахунок Pandoc AST vs сира XML не збігся (легко ламається текстовими рамками/legacy VML-картинками).
  - **Жодного автотесту** вірності імпорту в репозиторії — регресія пройде непоміченою.
  - **РІШЕННЯ (Анатолій, 2026-08-17)**: імпортер зараз НЕ чіпаємо. Приймаємо поточні втрати як відомий, задокументований компроміс — рухаємось далі по чеклісту. Дію на майбутнє додано окремим пунктом нижче: попередити автора в UI про ці обмеження (таблиці/виноски/розриви сторінок/авто-стилі), щоб він знав перевірити вручну після імпорту, а не дізнавався про втрату вже в друкованій книзі.
- [ ] UI-попередження при імпорті docx: показати автору короткий список того, що імпортер не переносить (таблиці, виноски, розриви сторінок, авто-визначення стилів розділ/епіграф/вірш/підпис) — конкретне місце в UI ще не визначено (сторінка завантаження рукопису чи after-import модалка), не в scope цього ТЗ, окрема задача
- [x] Розмір сторінки — **ОНОВЛЕНО (2026-08-17, пізніший прохід тієї ж сесії)**: попередня відповідь 145×205мм ("стандартний A5") — **застаріла**, замінена після звірки з офіційним ДСТУ 3018-95 (WebFetch) та рішенням підтримувати **жанр-залежний формат** (T-2059/T-2060), а не один фіксований розмір для всіх книг. Джерело правди тепер `PRINT_FORMATS`/`GENRE_TO_PRINT_FORMAT` у `packages/shared-types` (6 форматів ДСТУ 3018-95); `PRINT_TRIM_SIZE_MM`/`PRINT_TRIM_SIZE_LABEL` лишаються як **дефолт** (=`PRINT_FORMATS.standard`, 130×200мм, "Стандартний" — найпоширеніший формат художньої літератури), не єдиний можливий розмір. `Book.printWidthMm`/`printFormatKey` вже є в схемі й проставляються автоматично при збереженні жанру (`apps/api/src/modules/books/book.ts`, PATCH-хендлер). **Наслідок для контракту рендера нижче (розділ 1)**: `renderManuscriptPdf()` має брати ширину/висоту сторінки з `Book.printWidthMm`/`printHeightMm` (з фолбеком на `PRINT_TRIM_SIZE_MM`, якщо жанр/розмір ще не визначені), а НЕ зашивати один константний розмір у CSS `@page` — інакше нова жанр-залежна логіка форматів (T-2059/T-2060) не матиме ефекту на реальний друкований файл.
- [x] Спайк рендера — **вибір: WeasyPrint, тепер емпірично підтверджено на цільовому середовищі (2026-08-17)**. Раніше цей пункт спирався лише на документацію (не було локального Docker/Linux для перевірки). Перевірено в одноразовому `ubuntu:22.04`-контейнері на самому VPS (`docker run --rm ubuntu:22.04 ...`, той самий базовий образ, що й `apps/worker/Dockerfile`): встановлено WeasyPrint (apt-пакети + `pip3 install weasyprint`, `weasyprint --version` → 69.0), рендерено тестовий HTML з `.frontmatter { break-before: recto }` + `.chapter { break-before: recto }` → **3 сторінки** (форзац/recto → порожня verso → розділ/recto), що є коректною палітурною пагінацією — на відміну від Puppeteer/Chromium (той самий тест раніше давав 2 сторінки, правило ігнорувалось). Заразом виявлено й виправлено помилку в списку apt-пакетів воркер-Dockerfile: `libharfbuzz-subset0` не існує в репозиторіях Ubuntu 22.04 взагалі (apt падав), бракувало `libharfbuzz0b` (специфічно Ubuntu, не Debian) — виправлено.
- [x] `renderManuscriptPdf()` — **Реалізовано**: `apps/worker/src/lib/renderManuscriptPdf.ts`, приймає `content`/`widthMm`/`heightMm`/`pageNumberPosition`, шеллиться в `weasyprint` CLI (масив-форма `execFileSync`, не рядок-шел). `splitFrontMatter` перенесено в `packages/shared-types/src/manuscript/splitFrontMatter.ts` як чисту функцію (без DOM), як і планувалось; `extractOutlineFromDoc`/`buildTocContent` НЕ перенесені — TOC зі scope прибрано (розділ 2), лишились лише в клієнтському `paginateManuscript.ts`. Реальна кількість сторінок рахується окремим кроком через Ghostscript (`pdfpagecount`) над фінальним PDF, а не поверненням з `renderManuscriptPdf()` самої — функціонально еквівалентно контракту розділу 1, інша механіка.
- [x] Переклад `manuscriptProseStyles.tsx` у CSS `@page`-правила обраного рушія — **Реалізовано**: `packages/shared-types/src/manuscript/printHtml.ts` (нові `@page`/`break-before/after: recto/verso/page` правила поверх спільного `MANUSCRIPT_PROSE_CSS`, який лишається чисто презентаційним — без пагінаційної семантики, бо ним же користується живий редактор).
- [x] `generate-pdf-print.ts`: замінити крок LibreOffice-конвертації на `renderManuscriptPdf()`, Ghostscript CMYK-крок і запис `printPageCount`/`printPdfUrl` лишити без змін — **Реалізовано**, плюс новий `printPdfGeneratedAt` (нове поле+міграція) для визначення "застарілості" рендера відносно останнього редагування рукопису.
- [x] Детермінований jobId (`print-pdf-${bookId}`) + dedup-патерн (прецедент `manuscript.ts:38-53`) замість поточного випадкового jobId у `publishing.service.ts` — **Реалізовано**: `apps/api/src/modules/books/print-preview.ts`, той самий dedup-патерн (completed/failed → `.remove()` перед повторним `add()`).
- [x] Кнопка "Передперегляд" у редакторі рукопису (`manuscript/preview/page.tsx`) — тригерить той самий job + polling, замість клієнтського `ManuscriptPagePreview.tsx` — **Реалізовано 2026-08-17**.
- [x] Обидва входи передперегляду показують один і той самий `printPdfUrl` — **Реалізовано**: обидва входи (кнопка в редакторі + сайдбар-посилання) ведуть на той самий маршрут `manuscript/preview/page.tsx`, тож нема двох окремих UI, які треба тримати синхронізованими. (Примітка: окремого компонента `BookViewer.tsx`, згаданого в попередній версії цього пункту, у кодовій базі не існує — показ реалізовано напряму через `<iframe>`, найпростіший надійний спосіб показати PDF-файл у браузері, без вигадування нового великого компонента заради QA-фічі.)
- [x] Тимчасова кнопка "Завантажити PDF" у правій панелі (п.4 вище) — **Реалізовано**, посилання `target="_blank"` на той самий `printPdfUrl`, що й `<iframe>`.
- [ ] Живий тест: recto-старт розділу, епіграф-сам, розмір сторінки — усе звірено з реальним PDF (не з клієнтським наближенням; TOC поза scope цієї версії). **Частково закрито**: сам рушій (WeasyPrint) і ключове CSS-правило (`break-before: recto`) емпірично підтверджені в реальному цільовому середовищі (див. пункт "Спайк рендера" вище) — це усувало НАЙБІЛЬШИЙ невідомий ризик. **Не закрито**: повний прогін через РЕАЛЬНИЙ згенерований `buildManuscriptPrintHtml()` HTML із СПРАВЖНІМ рукописом (не синтетичний тест-документ) на задеплоєному воркері — потребує реального деплою цієї гілки, не відтворюється локально без Docker.
- [ ] Прибрати `measureNodes`/`paginateNodes`/`HTMLFlipBook` з клієнтського коду (лишити `useLivePageBreaks.ts` як орієнтовний оверлей) — **свідомо відкладено до живого тесту вище**: видаляти робочий клієнтський fallback до підтвердження нового пайплайна на реальних даних — ризик лишитись зовсім без робочого передперегляду, якщо там виявиться баг.
- [ ] Прибрати тимчасову кнопку "Завантажити PDF" після підтвердження — залежить від пункту вище.

---

## Чекліст живого тесту T-2056

Маршрут: кабінет автора → книга з `Текст рукопису.docx` → редактор рукопису → "Передперегляд" → `/dashboard/books/:id/manuscript/preview`.

- [ ] 1. TOC — шрифт body+2pt; номери сторінок біля пунктів реально відповідають фактичній сторінці заголовка (звірити 2-3 пункти вручну)
- [ ] 2. Розділ завжди з правої (непарної) сторінки
- [ ] 3. Епіграф завжди сам на сторінці, з обох боків
- [ ] 4. Fit-to-viewport — розворот влазить без скролу; ресайз вікна перераховує масштаб
- [ ] 5. Немає "сирітських" заголовків унизу сторінки
- [ ] 6. Палітурна конвенція: перший розворот після обкладинки — ліва порожня, права — титул
- [ ] 7. Загальний вигляд: номери по центру знизу, заголовок розділу зверху першої сторінки розділу
- [x] 8. **Розмір сторінки** — ВИРІШЕНО: жанр-залежний, дефолт 130×200мм ("Стандартний", ДСТУ 3018-95), див. чекліст реалізації вище

Звіт по кожному пункту: "ок" / "не ок + що саме", скріншот бажаний для п.1, п.2, п.8.
